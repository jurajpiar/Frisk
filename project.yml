name: ZipLook

options:
  bundleIdPrefix: org.deadkittens
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

# ZIPFoundation (D2) — pure-Swift zip reader, shared by the app and both extensions.
packages:
  ZIPFoundation:
    url: https://github.com/weichsel/ZIPFoundation
    from: "0.9.0"
  # Apple's Markdown parser (cmark-gfm) — used to render rich markdown previews in-app.
  swift-markdown:
    url: https://github.com/swiftlang/swift-markdown
    branch: main
  # Pure-Swift readers for non-zip archives (tar, gzip, bzip2, xz, 7z).
  SWCompression:
    url: https://github.com/tsolomko/SWCompression
    from: "4.8.0"

# Signing: DEVELOPMENT_TEAM is supplied by Config/Signing.xcconfig (git-ignored). Copy
# Config/Signing.xcconfig.example → Config/Signing.xcconfig and set your Apple Development
# Team ID. Automatic signing; a free personal team is sufficient (no paid membership).
configFiles:
  Debug: Config/Signing.xcconfig
  Release: Config/Signing.xcconfig

targets:
  ZipLook:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: ZipLook
    dependencies:
      - package: ZIPFoundation
      - package: SWCompression
      - package: swift-markdown
        product: Markdown
      - target: PreviewExtension       # Quick Look preview appex (stage 05)
      # File Provider appex (stage 07) is blocked by an extension sandbox limit on this
      # setup (see 07_file_provider/outputs/STATUS.md) — not embedded in the v1 app.
      # - target: FileProviderExtension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.deadkittens.ZipLook
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.0"
        CODE_SIGN_ENTITLEMENTS: ZipLook/ZipLook.entitlements
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_LANGUAGE: en-GB
    info:
      path: ZipLook/Info.plist
      properties:
        CFBundleDisplayName: ZipLook
        CFBundleDevelopmentRegion: en_GB
        LSApplicationCategoryType: public.app-category.utilities
        CFBundleDocumentTypes:
          - CFBundleTypeName: Archive
            CFBundleTypeRole: Viewer
            LSHandlerRank: Alternate
            LSItemContentTypes:
              - public.zip-archive
              - org.gnu.gnu-zip-archive
              - org.gnu.gnu-zip-tar-archive
              - public.bzip2-archive
              - public.tar-archive
              - org.7-zip.7-zip-archive
              - org.tukaani.xz-archive
        UTImportedTypeDeclarations:
          - UTTypeIdentifier: org.7-zip.7-zip-archive
            UTTypeDescription: 7-Zip Archive
            UTTypeConformsTo:
              - public.data
              - public.archive
            UTTypeTagSpecification:
              public.filename-extension:
                - 7z
          - UTTypeIdentifier: org.tukaani.xz-archive
            UTTypeDescription: XZ Archive
            UTTypeConformsTo:
              - public.data
            UTTypeTagSpecification:
              public.filename-extension:
                - xz
          - UTTypeIdentifier: org.gnu.gnu-zip-tar-archive
            UTTypeDescription: Gzip Tar Archive
            UTTypeConformsTo:
              - org.gnu.gnu-zip-archive
            UTTypeTagSpecification:
              public.filename-extension:
                - tgz
                - tar.gz

  PreviewExtension:
    type: app-extension
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: PreviewExtension
      - path: ZipLook/ZipCore
    dependencies:
      - package: ZIPFoundation
      - package: SWCompression
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.deadkittens.ZipLook.PreviewExtension  # D9
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.0"
        CODE_SIGN_ENTITLEMENTS: PreviewExtension/PreviewExtension.entitlements
        CODE_SIGN_STYLE: Automatic
    info:
      path: PreviewExtension/Info.plist
      properties:
        CFBundleDisplayName: ZipLook Preview
        CFBundleDevelopmentRegion: en_GB
        NSExtension:
          NSExtensionPointIdentifier: com.apple.quicklook.preview
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).PreviewProvider
          NSExtensionAttributes:
            QLIsDataBasedPreview: true
            QLSupportedContentTypes:
              - public.zip-archive
              - com.pkware.zip-archive
              - org.gnu.gnu-zip-archive
              - org.gnu.gnu-zip-tar-archive
              - public.bzip2-archive
              - public.tar-archive
              - org.7-zip.7-zip-archive
              - org.tukaani.xz-archive

  FileProviderExtension:
    type: app-extension
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: FileProviderExtension
      - path: ZipLook/ZipCore          # shared reader/model
    dependencies:
      - package: ZIPFoundation
      - package: SWCompression
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.deadkittens.ZipLook.FileProvider  # prefixed by host (D9)
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.0"
        CODE_SIGN_ENTITLEMENTS: FileProviderExtension/FileProviderExtension.entitlements
        CODE_SIGN_STYLE: Automatic
    info:
      path: FileProviderExtension/Info.plist
      properties:
        CFBundleDisplayName: ZipLook File Provider
        CFBundleDevelopmentRegion: en_GB
        NSExtension:
          NSExtensionPointIdentifier: com.apple.fileprovider-nonui
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).ZipFileProviderExtension
          NSExtensionFileProviderDocumentGroup: group.org.deadkittens.ziplook
          NSExtensionFileProviderSupportsEnumeration: true

  ZipLookTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: ZipLookTests
    dependencies:
      - target: ZipLook
      - package: ZIPFoundation
      - package: SWCompression
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.deadkittens.ZipLookTests
        GENERATE_INFOPLIST_FILE: "YES"
        SWIFT_VERSION: "5.0"
        CODE_SIGN_STYLE: Automatic

schemes:
  ZipLook:
    build:
      targets:
        ZipLook: all
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - ZipLookTests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
